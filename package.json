{
  "author": {
    "name": "Motionpicture co.,ltd."
  },
  "contributors": [
    {
      "name": "Tetsu Yamazaki",
      "email": "yamazaki@motionpicture.jp"
    }
  ],
  "dependencies": {
    "@motionpicture/sskts-domain": "^12.2.1",
    "debug": "^2.6.3",
    "moment": "^2.18.1"
  },
  "description": "This is a repogitory for SSKTS report jobs.",
  "devDependencies": {
    "@types/debug": "0.0.29",
    "@types/mocha": "^2.2.41",
    "@types/request-promise-native": "^1.0.2",
    "@types/underscore": "^1.8.0",
    "ink-docstrap": "^1.3.0",
    "jsdoc": "^3.4.3",
    "mocha": "^3.2.0",
    "nsp": "^2.6.3",
    "rimraf": "^2.6.1",
    "snyk": "^1.29.0",
    "tslint": "^4.5.1",
    "tslint-microsoft-contrib": "^4.0.1",
    "typescript": "^2.3.0",
    "underscore": "^1.8.3"
  },
  "engines": {
    "node": ">=6.9.1",
    "npm": ">=3.10.8"
  },
  "keywords": [],
  "license": "UNLICENSED",
  "name": "sskts-reportjobs",
  "private": true,
  "repository": {},
  "scripts": {
    "build": "tsc",
    "check": "tslint --type-check --project tsconfig.json -c tslint.json bin/**/*.ts && nsp check && snyk auth && snyk test --ignore-policy",
    "clean": "rimraf App_Data/**/*.js controller/**/*.js test/**/*.js docs/**/* npm-debug.log*",
    "jsdoc": "node_modules/.bin/jsdoc -c jsdoc.json",
    "test": "npm run check && mocha --no-timeouts --recursive",
    "preversion": "npm run clean && npm run build && npm test",
    "version": "git add -A",
    "postversion": "git push origin"
  },
  "version": "1.2.3"
}
